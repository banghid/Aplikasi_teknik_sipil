<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/dashboard_menu"
            android:icon="@drawable/ic_dashboard_24dp"
            android:title="Halaman Utama" />

        <item
            android:id="@+id/rapor_menu"
            android:icon="@drawable/ic_raport_24dp"
            android:title="Rapor" />
        <item
            android:id="@+id/developer_menu"
            android:icon="@drawable/ic_computer_24dp"
            android:title="Tentang Aplikasi" />
    </group>

    <item android:title="Akun">
        <menu>
            <item
                android:id="@+id/profile_menu"
                android:icon="@drawable/ic_profile_24dp"
                android:title="Profil Pengguna" />
            <item
                android:id="@+id/logout_menu"
                android:icon="@drawable/ic_directions_run_24dp"
                android:title="Logout" />
        </menu>
    </item>
</menu>